{
  "name": "youtube-digest",
  "version": "1.2.0",
  "private": true,
  "description": "Turn YouTube and Bilibili videos into learning resources with transcripts, an in-panel AI chat, and exportable notes.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "bash scripts/check-release.sh",
    "package": "bash scripts/package-extension.sh"
  }
}
